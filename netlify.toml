[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Ensure Netlify Forms are processed correctly
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
  conditions = {Role = ["admin"], Country = ["!*"]}

